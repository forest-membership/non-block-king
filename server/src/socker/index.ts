// import socketio from "socket.io";

// export default (server: any, options: Object) => {
//     const io = socketio(server, { ...options });

//     io.on("connection", (socket) => {
//         console.log("Client Connected");
//     });

//     return io;
// };
