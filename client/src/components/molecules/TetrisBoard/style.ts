import styled from 'styled-components';
import Block from '../../atoms/Block';

export const TetrisBoard = styled(Block)`
  width: fit-content;
  height: fit-content;
  border-radius: 2px;
  background-color: rgba(255, 255, 255, 0.2);
`;
